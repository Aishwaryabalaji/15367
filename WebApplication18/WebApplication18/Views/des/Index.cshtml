@model IEnumerable<WebApplication18.Models.de>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.desigid)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.desig)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.desigid)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.desig)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.desigid }) |
            @Html.ActionLink("Details", "Details", new { id = item.desigid }) |
            </td>
    </tr>
}

</table>
